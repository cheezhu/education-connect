# Education Connect V2 - 开发实施计划

## 项目概述

V2版本是对**团组管理功能的全面升级**，核心目标是将简单的团组列表升级为专业的团组管理系统。本计划提供清晰的开发路线图和技术实施方案。

## 开始前准备

### 环境检查
```bash
# 1. 确保V1版本正常运行
cd trip-manager/frontend && npm run dev  # 端口5173
cd trip-manager/backend && node simple-server.js  # 端口3001

# 2. 访问V1团组管理页面，了解现有功能
http://localhost:5173/groups

# 3. 检查代码结构
- frontend/src/pages/GroupManagement.jsx  # V1版本参考
- backend/simple-server.js  # 了解现有API结构
```

### 关键文件说明
- **不要修改**：V1的文件，保持向后兼容
- **需要创建**：V2的新文件，独立开发
- **可以参考**：V1的代码结构和样式

## 开发原则

1. **增量开发**：每个功能模块独立开发测试，避免大规模重构
2. **向后兼容**：V2功能不影响V1正常运行，用户可选择使用
3. **用户优先**：优先开发用户最需要的团组管理增强功能
4. **质量保证**：每个阶段都要有完整的测试和文档

## 三阶段开发计划

### 第一阶段：团组管理界面增强

**目标：实现专业的团组管理界面，替代V1的简单列表**

#### 步骤1: 团组列表V2页面开发
```javascript
// 文件位置: frontend/src/pages/GroupManagementV2.jsx

// 主要任务：
// 1. 创建卡片式团组展示组件
// 2. 实现筛选器（状态、类型、日期）
// 3. 添加搜索功能
// 4. 实现分页和排序

// 示例代码结构：
import React, { useState, useEffect } from 'react';
import { Card, Row, Col, Select, Input, Tag } from 'antd';
import { useNavigate } from 'react-router-dom';
import api from '../services/api';

const GroupManagementV2 = () => {
  const [groups, setGroups] = useState([]);
  const [filters, setFilters] = useState({
    status: 'all',
    type: 'all',
    search: ''
  });

  // 获取团组数据
  useEffect(() => {
    fetchGroups();
  }, [filters]);

  const fetchGroups = async () => {
    try {
      const response = await api.get('/v2/groups', { params: filters });
      setGroups(response.data);
    } catch (error) {
      console.error('获取团组失败:', error);
    }
  };

  // 渲染团组卡片
  const renderGroupCard = (group) => (
    <Card
      key={group.id}
      hoverable
      extra={<Tag color={statusColors[group.status]}>{group.status}</Tag>}
      actions={[
        <Button onClick={() => navigate(`/groups/v2/edit/${group.id}`)}>编辑</Button>,
        <Button>查看详情</Button>
      ]}
    >
      <Card.Meta
        title={group.name}
        description={
          <>
            <p>📅 {group.start_date} - {group.end_date}</p>
            <p>👥 学生: {group.student_count} | 老师: {group.teacher_count}</p>
            <p>📍 完成度: {group.completed_activities}/{group.activity_count}</p>
          </>
        }
      />
    </Card>
  );

  return (
    <div className="group-management-v2">
      {/* 筛选栏 */}
      <div className="filter-bar">
        <Select value={filters.status} onChange={val => setFilters({...filters, status: val})}>
          <Select.Option value="all">全部状态</Select.Option>
          <Select.Option value="准备中">准备中</Select.Option>
          <Select.Option value="进行中">进行中</Select.Option>
        </Select>
      </div>

      {/* 团组卡片列表 */}
      <Row gutter={[16, 16]}>
        {groups.map(group => (
          <Col xs={24} sm={12} lg={8} xl={6} key={group.id}>
            {renderGroupCard(group)}
          </Col>
        ))}
      </Row>
    </div>
  );
};
```

#### 步骤2: 团组编辑页面框架
```javascript
// 文件: GroupEditV2.jsx
主要任务：
1. 创建独立的编辑页面（非弹窗）
2. 实现三标签页布局
3. 完成团组概览标签页
4. 实现数据自动保存

技术要点：
- 使用 Ant Design Tabs 组件
- 使用 React Hook Form 管理表单
- 实现防抖自动保存
```

#### 步骤3: 后端API扩展
```javascript
// 文件: server-v2.js 或扩展 server.js
主要任务：
1. 扩展团组数据模型
2. 实现新的API端点
3. 添加数据验证
4. 实现筛选和搜索逻辑

技术要点：
- 保持与V1 API的兼容性
- 使用 express-validator 验证
- 实现高效的筛选算法
```

**第一阶段交付物**：
- ✅ 专业的团组列表界面
- ✅ 团组详细信息编辑功能
- ✅ 筛选、搜索、排序功能
- ✅ 完整的团组CRUD API

### 第二阶段：Google Calendar日程管理

**目标：实现专业级的日程管理功能，这是V2的核心创新**

#### 步骤4: 日历组件基础开发
```javascript
// 文件: CalendarDaysView.jsx
主要任务：
1. 创建动态天数网格布局
2. 实现时间轴（6:00-23:00）
3. 添加日期头部显示
4. 实现基础滚动和缩放

// 核心代码示例：
const CalendarDaysView = ({ group }) => {
  // 计算天数
  const calculateDays = (startDate, endDate) => {
    const start = new Date(startDate);
    const end = new Date(endDate);
    const days = [];

    for (let d = start; d <= end; d.setDate(d.getDate() + 1)) {
      days.push({
        date: new Date(d),
        dateStr: d.toISOString().split('T')[0],
        dayName: ['周日','周一','周二','周三','周四','周五','周六'][d.getDay()]
      });
    }
    return days;
  };

  // 生成时间槽（6:00-23:00，每30分钟）
  const generateTimeSlots = () => {
    const slots = [];
    for (let hour = 6; hour < 23; hour++) {
      slots.push(`${hour.toString().padStart(2, '0')}:00`);
      slots.push(`${hour.toString().padStart(2, '0')}:30`);
    }
    return slots;
  };

  const days = calculateDays(group.start_date, group.end_date);
  const timeSlots = generateTimeSlots();

  return (
    <div className="calendar-container">
      <div className="calendar-grid" style={{
        display: 'grid',
        gridTemplateColumns: `80px repeat(${days.length}, 1fr)`,
        gridTemplateRows: `60px repeat(${timeSlots.length}, 40px)`
      }}>
        {/* 渲染日期头部 */}
        <div className="corner-cell"></div>
        {days.map(day => (
          <div key={day.dateStr} className="date-header">
            <div>{day.date.getMonth()+1}月{day.date.getDate()}日</div>
            <div className="day-name">{day.dayName}</div>
          </div>
        ))}

        {/* 渲染时间轴和格子 */}
        {timeSlots.map(time => (
          <>
            <div className="time-label">{time}</div>
            {days.map(day => (
              <div
                className="time-slot"
                data-time={time}
                data-date={day.dateStr}
                onClick={() => handleSlotClick(day.dateStr, time)}
              />
            ))}
          </>
        ))}
      </div>
    </div>
  );
};
```

#### 步骤5: 活动管理功能
```javascript
// 文件: ActivityCard.jsx, TimeSlot.jsx
主要任务：
1. 实现点击创建活动
2. 活动卡片组件开发
3. 活动详情编辑弹窗
4. 颜色编码系统

技术要点：
- 使用 React DnD 或原生拖拽API
- 实现活动位置计算算法
- 处理时间重叠显示
```

#### 步骤6: 拖拽交互实现
```javascript
// 拖拽核心逻辑
主要任务：
1. 实现活动拖拽移动
2. 实现边缘拖拽调整时长
3. 添加拖拽预览效果
4. 实现拖拽限制和验证

关键技术：
- HTML5 Drag and Drop API
- 实时碰撞检测
- 15分钟精度控制
- 撤销/重做功能
```

#### 步骤7: 日程数据同步
```javascript
// 文件: scheduleAPI.js
主要任务：
1. 实现日程CRUD API
2. 冲突检测算法
3. 批量操作支持
4. 实时数据同步

技术要点：
- 乐观更新策略
- WebSocket 实时同步（可选）
- 冲突自动解决
```

**第二阶段交付物**：
- ✅ Google Calendar风格日程界面
- ✅ 完整的拖拽交互功能
- ✅ 时间冲突检测和处理
- ✅ 活动的CRUD操作

### 第三阶段：团员管理和优化

**目标：完善团员管理功能，优化整体性能**

#### 步骤8: 团员管理功能
```javascript
// 文件: MemberManagement.jsx
主要任务：
1. 团员列表表格
2. 团员信息编辑
3. 批量导入功能
4. 角色和权限管理

技术要点：
- Ant Design Table 高级功能
- Excel 导入导出
- 批量操作优化
```

#### 步骤9: 集成和优化
```javascript
主要任务：
1. 三个标签页数据联动
2. 性能优化（虚拟滚动、懒加载）
3. 错误处理和用户反馈
4. 响应式设计适配

优化重点：
- React.memo 优化渲染
- useMemo/useCallback 优化
- 代码分割和懒加载
```

#### 步骤10: 测试和文档
```
主要任务：
1. 单元测试编写
2. 集成测试
3. 用户手册编写
4. API文档完善
```

**第三阶段交付物**：
- ✅ 完整的团员管理功能
- ✅ 性能优化达标
- ✅ 完整的测试覆盖
- ✅ 用户和开发文档

## 技术架构设计

### 前端架构
```
src/
├── pages/
│   ├── GroupManagementV2/
│   │   ├── index.jsx          # 主页面
│   │   ├── GroupCard.jsx      # 团组卡片
│   │   └── FilterBar.jsx      # 筛选栏
│   └── GroupEditV2/
│       ├── index.jsx          # 编辑主页面
│       ├── GroupOverview.jsx  # 概览标签
│       ├── MemberManage.jsx   # 团员标签
│       └── ScheduleManage/    # 日程标签
│           ├── index.jsx
│           ├── CalendarDaysView.jsx
│           ├── ActivityCard.jsx
│           └── TimeSlot.jsx
├── services/
│   ├── apiV2.js              # V2 API客户端
│   └── scheduleService.js    # 日程服务
└── hooks/
    ├── useGroupData.js       # 团组数据Hook
    └── useDragDrop.js        # 拖拽Hook
```

### 后端架构
```
backend/
├── routes/
│   ├── groupsV2.js          # V2团组路由
│   ├── members.js           # 团员路由
│   └── schedules.js         # 日程路由
├── models/
│   ├── GroupV2.js          # V2团组模型
│   ├── Member.js           # 团员模型
│   └── Schedule.js         # 日程模型
├── services/
│   ├── conflictChecker.js  # 冲突检测
│   └── dataValidator.js    # 数据验证
└── server-v2.js            # V2服务入口
```

## 关键技术决策

### 1. 日历组件选择
- **方案A**：基于 FullCalendar 定制 ❌
- **方案B**：基于 React Big Calendar ❌
- **方案C**：完全自定义开发 ✅
- **原因**：需要精确控制布局和交互，第三方组件限制太多

### 2. 拖拽库选择
- **方案A**：react-beautiful-dnd ❌
- **方案B**：react-dnd ❌
- **方案C**：HTML5 原生 Drag & Drop ✅
- **原因**：更灵活，性能更好，无额外依赖

### 3. 状态管理
- **方案A**：Redux ❌
- **方案B**：MobX ❌
- **方案C**：React Hooks + Context ✅
- **原因**：项目规模适中，Hooks足够用

## 风险和应对

| 风险 | 可能性 | 影响 | 应对措施 |
|-----|-------|------|---------|
| 日历组件复杂度高 | 高 | 高 | 先做简单原型验证，逐步增加功能 |
| 拖拽性能问题 | 中 | 高 | 使用虚拟滚动，限制同时显示的活动数 |
| 数据同步冲突 | 中 | 中 | 实现乐观更新和冲突解决机制 |
| 浏览器兼容性 | 低 | 中 | 明确支持Chrome/Edge/Firefox最新版 |

## 测试策略

### 单元测试
- 工具：Jest + React Testing Library
- 覆盖率目标：核心逻辑 > 80%

### 集成测试
- 工具：Cypress
- 测试场景：完整的用户操作流程

### 性能测试
- 工具：React DevTools Profiler
- 目标：拖拽响应 < 100ms，页面加载 < 2s

## 部署计划

### 开发环境
- 分支策略：feature/v2-group-management
- 持续集成：每次提交运行测试

### 测试环境
- 独立的V2测试服务器
- 模拟真实数据测试

### 生产部署
- 灰度发布：先开放给部分用户
- 监控：添加性能和错误监控
- 回滚方案：保留V1入口，可随时切换

## 项目里程碑

- **M1**：团组管理V2界面完成，可以查看和编辑团组信息
- **M2**：日程管理功能完成，实现Google Calendar体验
- **M3**：全部功能完成，通过测试，准备上线

## 开发规范

### 代码规范
- ESLint + Prettier 统一代码风格
- 组件命名：PascalCase
- 文件命名：与组件名一致
- Git提交：feat/fix/docs/style/refactor

### 注释规范
```javascript
/**
 * 计算团组行程天数
 * @param {string} startDate - 开始日期 YYYY-MM-DD
 * @param {string} endDate - 结束日期 YYYY-MM-DD
 * @returns {number} 天数
 */
function calculateDays(startDate, endDate) {
  // 实现逻辑
}
```

### 组件规范
```javascript
// 使用 TypeScript 或 PropTypes
CalendarDaysView.propTypes = {
  group: PropTypes.shape({
    id: PropTypes.number.isRequired,
    start_date: PropTypes.string.isRequired,
    end_date: PropTypes.string.isRequired
  }).isRequired
};
```

## 交付检查清单

### 功能检查
- [ ] 团组列表卡片显示正确
- [ ] 筛选和搜索功能正常
- [ ] 团组编辑页面三标签切换流畅
- [ ] 日历显示正确的天数
- [ ] 拖拽创建活动正常
- [ ] 拖拽移动活动正常
- [ ] 时长调整功能正常
- [ ] 冲突检测和提示正确
- [ ] 团员管理CRUD正常
- [ ] 数据保存和同步正常

### 性能检查
- [ ] 页面加载时间 < 2秒
- [ ] 拖拽响应时间 < 100ms
- [ ] 支持100+活动流畅显示
- [ ] 内存使用稳定

### 质量检查
- [ ] 代码通过ESLint检查
- [ ] 单元测试覆盖率 > 80%
- [ ] 无控制台错误
- [ ] 响应式设计正常

---

*本计划为V2版本开发指南，请严格按照计划执行，确保高质量交付。*